% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_call.R
\name{cluster_call}
\alias{cluster_call}
\title{cluster_call}
\usage{
cluster_call(
  cl,
  X,
  FUN,
  ...,
  type = c("lapply", "sapply", "apply"),
  MARGIN = NULL,
  simplify = TRUE,
  USE.NAMES = TRUE,
  LB = FALSE,
  export = NULL,
  packages = NULL
)
}
\arguments{
\item{cl}{A cluster created with [parallel::makeCluster()].}

\item{X}{Input object.}

\item{FUN}{Function to evaluate.}

\item{...}{Named objects to assign on workers and extra arguments passed to FUN.}

\item{type}{One of `"lapply"`, `"sapply"`, or `"apply"`.}

\item{MARGIN}{For `type="apply"` only. Passed to [base::apply()].}

\item{simplify}{Logical; simplify result for `"sapply"`.}

\item{USE.NAMES}{Logical; keep names for `"sapply"`.}

\item{LB}{Logical; use load-balanced scheduling (`clusterApplyLB`).}

\item{export}{Optional character vector of objects to export from caller env.}

\item{packages}{Optional additional packages to load on workers.}
}
\value{
Result identical to the corresponding parallel apply function.
}
\description{
Persistent Parallel Apply on PSOCK Clusters
}
\details{
A safe and fast wrapper around the parallel apply family that avoids
exporting large closures. Objects are assigned once per call to each worker,
preventing scoping bugs and dramatically improving performance when running
parallel code inside functions.

The package namespace of the caller is automatically loaded on workers so
internal helpers and S3 methods are available.


Workers are persistent R sessions. This function:
\enumerate{
  \item loads required packages,
  \item assigns provided objects once,
  \item evaluates a lightweight function in parallel.
}

This avoids repeated serialization of parent environments, a common cause of
extremely slow parallel execution inside functions.
}
\examples{
\dontrun{
cl <- parallel::makeCluster(4)

A <- rnorm(1000)
f <- function(i) A[i]^2

cluster_call(cl, 1:1000, f, A=A)
cluster_call(cl, 1:1000, f, A=A, type="sapply")

M <- matrix(rnorm(1000), ncol=10)
cluster_call(cl, M, rowMeans, type="apply", MARGIN=1)

parallel::stopCluster(cl)
}

}
